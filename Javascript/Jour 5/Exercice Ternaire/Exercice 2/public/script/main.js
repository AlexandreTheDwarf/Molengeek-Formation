//  Exercice 1 - Vérification de l'âge pour accéder à un contenu

    // let year = prompt("Donne moi ta date de naissance")

    // year = Number(year)

    // let age = 2024 - year

    // let message = age <= 13 ?  alert("Accès refusé") : (age > 13 && age <= 17 )?  alert("Accès limité") : alert("Accès complet") 

//  Exercice 2 - Vérification des notes

    // let note = prompt("Donne moi ta note moyenne")

    // note = Number(note)

    // let message = 
    //     note < 50 ? "Échec" : 
    //     note <= 64 ? "Passable" : 
    //     note <= 84 ? "Bien" : 
    //     note >= 85 ? "Excellent" : 
    //     "J'ai dit ta note pas une bêtise"

    // alert(message)

//  Exercice 3 - Vérification de connexion et d'abonnement

    // let login = prompt("Es tu connecté  ? oui ou non ?")

    // let sub = prompt("Es tu abonné ? oui ou non ")

    // let message =  (login == "oui" && sub == "non") ? "Abonnez-vous pour accéder au contenu" : login == "non" ? "Veuillez vous connecter" : "Bienvenue sur la plateforme"

    // alert(message)