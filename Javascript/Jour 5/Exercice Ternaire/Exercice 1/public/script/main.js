// Exercice 1 - Pair ou impair

    // let nbr = prompt("Donne moi un chiffre")

    // let message = nbr%2==0 ? alert("Le nombre est pair") : alert("Le nombre est impair");

// Exercice 2 - Accès autorisé ou refusé

    // let age = prompt("En quelle année es tu né ?")

    // let result = 2024 - Number(age)

    // let message = result >= 18 ? alert("Accès autorisé") : alert("Accès refusé")

// Exercice 3 - Valeur par défaut

    // let word = prompt("Donne moi un mot")

    // let message = word.length > 0 ? alert("Bienvenue Pierre") : alert("Valeur indéfini")

// Exercice 4 - Plus grand nombre

    // const nbr1 = 5
    // const nbr2 = 8

    // let result = Math.max(nbr1, nbr2)

    // let message = result == nbr2 ? alert(nbr2 + " est le plus grand") : alert(nbr1 + " est le plus grand")

// Exercice 5 - Statut de connexion

    // let user = prompt("Quelle est votre login ?")

    // let psw = prompt("Quelle est votre mot de passe ?")

    // let isLoggedIn 

    // if (user == "Admin"  && psw == "Admin"){
    //     isLoggedIn = "Connecté"
    // }
    // else{
    //     isLoggedIn = "Déconnecté"
    // }

    // let message = isLoggedIn === "Connecté" ? alert("Bienvenue Admin") : alert("Vous n'êtes pas connecté")

// Exercice 6 - Appliquer un style

    
// Exercice 7 - Classification des notes

    // let note = prompt("Donne moi ta note")

    // let message = note >= 90 ? alert("Bravo tu as eu un A") : (note <= 89 &&  note >= 80) ? alert("Pas mal tu as eu un B") : alert("C peut mieux faire")

// Exercice 8 - Vérifier un mot dans un tableau

    // let elden = ["melina", "ranni", "fia", "hyetta"]

    // let choice = prompt("Qui est selon toi la plus jolie fille d'elden ring ?")

    // choice = choice.toLowerCase()

    // let message = elden.includes(choice) ? alert("Je suis d'accord avec toi") : alert("Tu as de la merde dans les yeux ou quoi ?")

// Exercice 9 - Réduction de chaîne

    // let sentence = prompt("Donne moi une phrase")

    // let message = sentence.length >= 15 ? sentence.slice(0,16) + " ..." : sentence

    // alert(message)

//  Exercice 10 - Nombre positif, négatif ou zéro

    // let nbr = prompt("Donne moi un nombre")

    // nbr = Number(nbr)

    // let message = Math.sign(nbr) == 1 ?  alert("Positif") : Math.sign(nbr) == -1 ? alert("Negatif") : alert("Zéro") 